{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Iniciar API + Abrir Swagger no Firefox",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "url": "https://localhost/swagger",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "run-api"
    }
  ]
}
